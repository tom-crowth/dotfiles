prefix C-a

set-window-option -g mode-keys vi
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'v' begin-selection

bind-key -Tcopy-mode-vi 'C-v' send -X begin-selection
bind-key -Tcopy-mode-vi 'C-v' send -X copy-selection

# start at 1 base on keyboard layout
set -g base-index 1
